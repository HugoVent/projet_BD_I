L'outil de compilation fonctionne en ligne de commande en suivant le procédé suivant : 
-Entrer la base de données sur laquelle les requêtes vont être effectuées 
(attention, si la base de données est dans un autre répertoire courant, il est nécéssaire d'indiquer le chemin en entier)